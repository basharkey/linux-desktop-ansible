---
- name: Change debian release to testing and add non-free repositories
  template:
    src: sources.list.j2
    dest: /etc/apt/sources.list
    mode: '0644'
  become: yes

- name: Upgrade current packages to new release
  apt:
    upgrade: full
    update_cache: yes

- name: Configure apt to not install recommended packages
  lineinfile:
    path: /etc/apt/apt.conf.d/01norecommends
    line: 'APT::Install-Recommends "0";'
    state: present
    create: true
    mode: '0644'
  become: yes

- name: Install debian packages
  apt:
    name: "{{ debian_packages }}"
    state: present
    install_recommends: no
  become: yes

- name: Enable flathub repository
  flatpak_remote:
    name: flathub
    state: present
    flatpakrepo_url: "https://flathub.org/repo/flathub.flatpakrepo"
    method: user

- name: Install flatpak packages
  flatpak:
    name: "{{ flatpak_packages }}"
    state: present
    method: user
